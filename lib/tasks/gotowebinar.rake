#Task to create a file by the name of gotowebinar_credentials.rb in the initializers folder

namespace :gotowebinar do
  
  desc "Create a gotowebinar_credentials.rb file in Rails Initializers directory"
  task :install => :enviornment do
    
      puts "So far this is working great"
    
  end
  
  
end



